Start the project by running: `./vendor/bin/sail up -d` and `npm run dev` (if not started automatically)

Visit `http://localhost:8000` to access the application.

Stop the project by running: `./vendor/bin/sail down`

Test Credentials
Student: [student@bitbyworld.com] / password
Admin: [admin@bitbyworld.com] / password
